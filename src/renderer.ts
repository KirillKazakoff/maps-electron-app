import './UI/indexReact';

const renderer = async () => {
    // const path = await window.electronAPI.api.getPath();
    // console.log(path);
    // await window.electronAPI.api.getDefaultSettings();
};

renderer();
